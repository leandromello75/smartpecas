# ==========================================================
# SmartPeças ERP - .env
# =============================================================================
# Arquivo: /smartpecas/.env
# ARQUIVO .env DE PRODUÇÃO
# ==========================================================

NODE_ENV=production
PORT=3000

# ✅ Use os valores exatos para usuário, senha e nome do banco
POSTGRES_USER=postgres
POSTGRES_PASSWORD=q1w2e3r4
POSTGRES_DB=smartpecas_db

# ✅ Garanta que esta URL esteja correta e use as variáveis acima
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}?schema=public"

# ✅ Use um segredo longo e aleatório para o JWT
JWT_SECRET=BTKBm59FfC4FsVEmNvsP8EU8ewxa9eEb
JWT_EXPIRES_IN=1d # Ex: 1 dia

# ✅ Se o seu frontend já estiver no ar, coloque a URL dele aqui
#FRONTEND_URL=http://seusite.com.br

# ----------------------------------------------------------
# Configurações de Cache Redis
# ----------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
CACHE_TTL=300 # Tempo de vida padrão do cache em segundos
